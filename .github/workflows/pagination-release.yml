name: Pagination release

# Publishes BuildingBlocks.Pagination.EntityFrameworkCore when a pagination-v* tag is pushed.
# Tag format: pagination-vX.Y.Z must match <Version> in the EF csproj.

on:
  release:
    types: [published]
  push:
    tags:
      - "pagination-v*"
  workflow_dispatch:
    inputs:
      publish_nuget:
        description: "Push to nuget.org (requires Trusted Publishing + NUGET_USER)"
        type: boolean
        default: false

jobs:
  release:
    if: ${{ github.event_name == 'workflow_dispatch' || startsWith(github.ref_name, 'pagination-v') }}
    runs-on: ubuntu-latest
    environment: nuget
    permissions:
      id-token: write
      contents: write
    env:
      NUGET_USER: ${{ secrets.NUGET_USER }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            9.0.x
            10.0.x

      - name: Resolve version
        id: ver
        shell: bash
        run: |
          set -euo pipefail
          version_of() { sed -n 's:.*<Version>\(.*\)</Version>.*:\1:p' "$1" | head -1; }
          EF=$(version_of src/BuildingBlocks/Pagination.EntityFrameworkCore/BuildingBlocks.Pagination.EntityFrameworkCore.csproj)
          echo "project=$EF" >> "$GITHUB_OUTPUT"
          REF="${GITHUB_REF_NAME}"
          if [[ "$GITHUB_REF" == refs/tags/* ]]; then
            TAG="$REF"
            STRIPPED="${TAG#pagination-v}"
            echo "tag=$TAG" >> "$GITHUB_OUTPUT"
            echo "from_tag=$STRIPPED" >> "$GITHUB_OUTPUT"
            if [[ "$STRIPPED" != "$EF" ]]; then
              echo "::error::Tag version ($STRIPPED) does not match EF <Version> ($EF)"
              exit 1
            fi
          else
            echo "tag=" >> "$GITHUB_OUTPUT"
            echo "from_tag=" >> "$GITHUB_OUTPUT"
          fi

      - name: Test
        run: |
          dotnet test tests/BuildingBlocks/Pagination.Tests/BuildingBlocks.Pagination.Tests.csproj -c Release --verbosity minimal
          dotnet test tests/BuildingBlocks/Pagination.EntityFrameworkCore.Tests/BuildingBlocks.Pagination.EntityFrameworkCore.Tests.csproj -c Release --verbosity minimal
          dotnet test tests/BuildingBlocks/Pagination.Dapper.Tests/BuildingBlocks.Pagination.Dapper.Tests.csproj -c Release --verbosity minimal

      - name: Pack
        run: dotnet pack src/BuildingBlocks/Pagination.EntityFrameworkCore/BuildingBlocks.Pagination.EntityFrameworkCore.csproj -c Release -o artifacts/nuget -p:ContinuousIntegrationBuild=true

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: nuget-packages-release
          path: |
            artifacts/nuget/*.nupkg
            artifacts/nuget/*.snupkg
          if-no-files-found: error

      - name: Attach packages to GitHub Release
        if: github.event_name == 'release'
        continue-on-error: true
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            artifacts/nuget/*.nupkg
            artifacts/nuget/*.snupkg

      - name: Create GitHub Release for tag
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@a06a81a03ee405af7f2048a818ed3f03bbf83c7b # v2
        with:
          tag_name: ${{ github.ref_name }}
          name: BuildingBlocks.Pagination.EntityFrameworkCore ${{ steps.ver.outputs.project }}
          generate_release_notes: true
          files: |
            artifacts/nuget/*.nupkg
            artifacts/nuget/*.snupkg

      - name: NuGet login (Trusted Publishing OIDC)
        if: ${{ env.NUGET_USER != '' && (github.event_name != 'workflow_dispatch' || inputs.publish_nuget) }}
        id: login
        uses: NuGet/login@v1
        with:
          user: ${{ env.NUGET_USER }}

      - name: Push to nuget.org
        if: ${{ env.NUGET_USER != '' && (github.event_name != 'workflow_dispatch' || inputs.publish_nuget) && steps.login.outcome == 'success' }}
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob
          for pkg in artifacts/nuget/*.nupkg; do
            dotnet nuget push "$pkg" \
              --api-key "${{ steps.login.outputs.NUGET_API_KEY }}" \
              --source https://api.nuget.org/v3/index.json \
              --skip-duplicate
          done
          for spkg in artifacts/nuget/*.snupkg; do
            dotnet nuget push "$spkg" \
              --api-key "${{ steps.login.outputs.NUGET_API_KEY }}" \
              --source https://api.nuget.org/v3/index.json \
              --skip-duplicate || true
          done

      - name: Dry-run notice
        if: ${{ env.NUGET_USER == '' || (github.event_name == 'workflow_dispatch' && !inputs.publish_nuget) }}
        run: |
          echo "Skipped nuget.org push (NUGET_USER unset or publish_nuget=false)."
